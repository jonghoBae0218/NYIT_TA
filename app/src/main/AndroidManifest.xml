<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NYITTutorAssistant"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.NYITTutorAssistant">
            <intent-filter> <!-- 20-24 line이 속해있는 activity를 실행시켜줌 -->
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AskQActivity" />
        <activity
            android:name=".QuestionPage"
            android:windowSoftInputMode="adjustPan"
            />
        <activity android:name=".BrowseQuestionsActivity" />
        <activity android:name=".BrowseTitleActivity" />
        <activity android:name=".QuestionViewActivity" />
    </application>

</manifest>